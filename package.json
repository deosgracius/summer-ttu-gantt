{
  "name": "summer-timeline",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Multi-tenant Gantt & Budget tracker — test harness for core logic",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "wrangler": "^4.112.0"
  }
}
