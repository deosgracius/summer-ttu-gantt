# Files in the repo root that should NOT be publicly served by Cloudflare.
# Only index.html (the app) needs to be reachable.
*.md
*.pdf
.gitignore
.assetsignore
.github/
package.json
package-lock.json
node_modules/
*.test.js
vitest.config.*
